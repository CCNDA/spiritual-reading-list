const API = '/api';
let currentCategory = '';
let searchTimer = null;
document.addEventListener('DOMContentLoaded',()=>{loadBooks();document.getElementById('categoryTabs').addEventListener('click',e=>{const b=e.target.closest('.tab');if(!b)return;document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));b.classList.add('active');currentCategory=b.dataset.slug;loadBooks();});document.getElementById('searchInput').addEventListener('input',e=>{clearTimeout(searchTimer);searchTimer=setTimeout(()=>loadBooks(e.target.value),350);});document.getElementById('modalOverlay').addEventListener('click',closeModal);});async function loadBooks(q=''){const g=document.getElementById('bookGrid');g.innerHTML='<p class="loading">我入中...</p>';const u=q?`${API}/books?q=${encodeURIComponent(q)}`:`${API}/books${currentCategory?'?category='+currentCategory:''}`;const b=await fetch(u).then(r=>r.json()).catch(()=>[]);if(!b.length){g.innerHTML='<p class="empty">不前涨有書目</p>';return;}g.innerHTML=b.map(x=>`<div class="book-card" onclick="openBook(${x.id})">${x.cover_image?`<img src="${x.cover_image}" alt="${x.title}">`:''}<div class="book-cat">${x.category_name||x.category||''}</div><div class="book-title">${x.title}</div><div class="book-author">${x.author}</div>${x.recommended_by?`<div class="book-rec">推薦：${x.recommended_by}</div>`:''}</div>`).join('');}function closeModal(){document.getElementById('modal').classList.add('hidden');}